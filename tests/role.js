var app = require('../index');
var request = require('supertest')(app);
var expect = require('chai').expect;
var Role = require('../server/models/role.js');

describe('Role test suite' function(){
  var token;
  expect(true).to.equal(true);
}
